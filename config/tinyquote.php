<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Cache Settings
    |--------------------------------------------------------------------------
    |
    | Prefix and TTL settings applied when storing cache entries for this
    | package. The "cache_prefix" helps avoid naming conflicts with other
    | parts of the application, while "cache_ttl" defines how long (in
    | seconds) cached data should remain valid.
    |
    */

    'cache_prefix' => 'tinyquote_cache',

    'cache_ttl' => 3600,

];
